# Dependencies
node_modules/
packages/**/node_modules/

# Build output
dist/
build/
packages/**/dist/
out/
release/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# Environment & secrets
.env
.env.*
!.env.example
*.pem
*.key

# Editor / OS
.DS_Store
Thumbs.db
.idea/
*.swp
.vscode/*
!.vscode/extensions.json

# Electron-builder / cache
.cache/
*.tsbuildinfo

# Local-only Obsidian "brain" vault — private project context, NEVER published.
vault/
# Obsidian local config (created when opening a folder as a vault).
.obsidian/
# local agent local project config (launch.json, local settings).
.claude/
# local agent session instructions (machine paths + contact info) — local only.
CLAUDE.md
